/* PYTHON·LAB — interactive cheat sheet. */

export const cheatSheet = [
{ cat: "Basics", topic: "print()", syntax: `print("Hello")\nprint("a", "b", sep="-")`, explanation: "Prints values to the output. Multiple values are separated by spaces by default; change it with sep.", when: "Showing results, debugging values, simple reports.", example: `print("Total:", 3 + 4)\nprint("a", "b", "c", sep="-")`, mistake: `print "Hello"   # Python 3 needs parentheses` },
{ cat: "Basics", topic: "Variables", syntax: `name = "Ana"\nage = 21`, explanation: "A name bound to a value with =. Create it by assigning; reassigning changes it.", when: "Storing anything you need later: user data, counters, results.", example: `score = 10\nscore = score + 5\nprint(score)`, mistake: `21 = age   # name must be on the left` },
{ cat: "Basics", topic: "Comments", syntax: `# single line\n"""multi\nline"""`, explanation: "Text ignored by Python. Use # for notes and to disable code temporarily.", when: "Explaining WHY code exists, leaving TODOs.", example: `# add 1 to the counter\ncount = 0\ncount += 1  # same as count = count + 1`, mistake: `Forgetting the # makes Python try to run your note.` },
{ cat: "Basics", topic: "Data Types", syntax: `int, float, str, bool, None`, explanation: "The kind of value: 3 is int, 3.0 is float, \"3\" is str, True is bool, None means nothing.", when: "Understanding errors and choosing the right operations.", example: `print(type(3), type(3.0), type("3"), type(True))`, mistake: `age = "25"  # a string — age + 1 crashes` },
{ cat: "Basics", topic: "Type Conversion", syntax: `int("21"), float("3.5"), str(21), bool(0)`, explanation: "Converts a value to another type. Fails with ValueError when the text is not a valid number.", when: "After input(), when mixing text and numbers, cleaning data.", example: `print(int("21") + 1)\nprint(str(21) + "!")`, mistake: `int("abc")  # ValueError` },
{ cat: "Basics", topic: "f-strings", syntax: `f"Hello {name}"\nf"{price:.2f}"`, explanation: "Embeds variables and expressions inside text. Supports formatting like .2f (2 decimals) or >10 (right-align).", when: "Building readable output messages.", example: `name, price = "Tea", 2.5\nprint(f"{name}: {price:.2f}")`, mistake: `"Hello " + age  # TypeError; use f"Hello {age}"` },
{ cat: "Basics", topic: "input()", syntax: `name = input("Name: ")\nage = int(input("Age: "))`, explanation: "Reads a typed line, always as a string. Convert with int()/float() for math.", when: "Interactive console programs (on a real computer; this sandbox simulates it).", example: `# name = input("Your name: ")\nname = "Ana"\nprint(f"Hi {name}")`, mistake: `age = input()\nprint(age + 1)  # TypeError: convert with int()` },
{ cat: "Operators", topic: "Arithmetic", syntax: `+  -  *  /  //  %  **`, explanation: "/ gives float, // floors, % is the remainder, ** is power.", when: "All math: totals, remainders, even/odd checks, powers.", example: `print(7 / 2, 7 // 2, 7 % 2, 2 ** 3)`, mistake: `5 / 0  # ZeroDivisionError` },
{ cat: "Operators", topic: "Comparison", syntax: `==  !=  >  <  >=  <=`, explanation: "Compare two values, result is True/False. == compares, = assigns.", when: "Conditions, searching, validation.", example: `print(3 == 3, 3 != 3, 5 >= 5)`, mistake: `if age = 18:  # SyntaxError: use ==` },
{ cat: "Operators", topic: "Logical", syntax: `and   or   not`, explanation: "Combine booleans: and needs both, or needs one, not flips.", when: "Complex conditions like ranges or multi-field checks.", example: `age = 25\nprint(age > 18 and age < 65)`, mistake: `if age > 18 or < 65:  # second side needs its own condition` },
{ cat: "Operators", topic: "Assignment", syntax: `x += 1   x -= 2   x *= 3   x /= 2`, explanation: "Shorthand operators: x += 1 means x = x + 1.", when: "Counters and accumulators inside loops.", example: `total = 0\ntotal += 10\ntotal *= 2\nprint(total)`, mistake: `x =+ 1  # typo: order matters, it is +=` },
{ cat: "Operators", topic: "Membership: in", syntax: `"a" in word\nitem in items`, explanation: "True when a value is found in a string, list, tuple, dict (checks keys) or set.", when: "Search checks, filters, validation.", example: `word = "python"\nprint("py" in word, "z" not in word)`, mistake: `Using == to search a list; use the in operator.` },
{ cat: "Control Flow", topic: "if", syntax: `if condition:\n    # code`, explanation: "Runs the indented block only when the condition is True.", when: "Doing something only under a condition.", example: `age = 20\nif age >= 18:\n    print("Adult")`, mistake: `if age >= 18\n    print("Adult")  # missing colon` },
{ cat: "Control Flow", topic: "if / else", syntax: `if cond:\n    ...\nelse:\n    ...`, explanation: "Two mutually exclusive paths — exactly one block runs.", when: "Yes/no decisions.", example: `score = 55\nif score >= 60:\n    print("Pass")\nelse:\n    print("Fail")`, mistake: `else without a colon.` },
{ cat: "Control Flow", topic: "elif", syntax: `if a:\n    ...\nelif b:\n    ...\nelse:\n    ...`, explanation: "Extra conditions checked top-to-bottom; the first True wins and the rest are skipped.", when: "Grades, menus, categories with many cases.", example: `score = 83\nif score >= 90: print("A")\nelif score >= 80: print("B")\nelse: print("C")`, mistake: `Putting the most general condition first makes later ones unreachable.` },
{ cat: "Control Flow", topic: "for + range()", syntax: `for i in range(5):\n    print(i)`, explanation: "Repeats once per value. range(a, b, step) counts from a to b-1 by step.", when: "Counting, fixed repetitions, iterating numbers.", example: `for i in range(1, 4):\n    print(i)`, mistake: `range(5) gives 0..4, not 1..5 — use range(1, n + 1)` },
{ cat: "Control Flow", topic: "for over collection", syntax: `for item in items:\n    print(item)`, explanation: "Visits each element of a list, string, tuple, dict or generator.", when: "Processing every element — the most common loop.", example: `for fruit in ["apple", "kiwi"]:\n    print(fruit)`, mistake: `for i in items:\n    print(items)  # prints the whole list each time` },
{ cat: "Control Flow", topic: "while", syntax: `while condition:\n    # body`, explanation: "Repeats while the condition stays True. Something in the body must eventually make it False.", when: "Unknown number of repeats: games, input validation.", example: `count = 0\nwhile count < 3:\n    print(count)\n    count += 1`, mistake: `Forgetting count += 1 → infinite loop` },
{ cat: "Control Flow", topic: "break", syntax: `while True:\n    if done: break`, explanation: "Exits the nearest loop immediately.", when: "Early exit: found the item, reached a stop condition.", example: `for i in range(10):\n    if i == 3:\n        break\n    print(i)`, mistake: `break outside a loop  # SyntaxError` },
{ cat: "Control Flow", topic: "continue", syntax: `if skip: continue`, explanation: "Skips the rest of this turn and jumps to the next iteration.", when: "Filtering out unwanted values inside a loop.", example: `for i in range(5):\n    if i % 2 == 0:\n        continue\n    print(i)`, mistake: `Confusing continue (skip one turn) with break (stop all).` },
{ cat: "Data Structures", topic: "Lists", syntax: `items = [1, 2, 3]\nitems[0]   # 1\nitems[-1]  # 3`, explanation: "Ordered, changeable collection. Index from 0; negative counts from the end.", when: "Any ordered collection: scores, names, tasks.", example: `items = [1, 2, 3]\nitems.append(4)\nprint(items, len(items))`, mistake: `items(0)  # indexing uses square brackets` },
{ cat: "Data Structures", topic: "List methods", syntax: `append, insert, remove, pop, sort, reverse`, explanation: "append adds at the end, insert at a position, remove deletes a value, pop removes & returns, sort orders in place.", when: "Growing, shrinking and ordering lists.", example: `items = [3, 1]\nitems.append(2)\nitems.sort()\nprint(items)`, mistake: `result = items.sort()  # sort() returns None; it changes items in place` },
{ cat: "Data Structures", topic: "Slicing", syntax: `items[a:b]\nitems[:3]  items[::-1]`, explanation: "Takes a section from index a up to (not including) b. Step can be negative to reverse.", when: "First N items, copies, reversals, pagination.", example: `nums = [0, 1, 2, 3, 4]\nprint(nums[1:4], nums[::-1])`, mistake: `Expecting nums[2:5] to include index 5 — the end is excluded.` },
{ cat: "Data Structures", topic: "Tuples", syntax: `point = (3, 5)\nx, y = point`, explanation: "Like a list but immutable — cannot be changed after creation. Unpacks neatly into variables.", when: "Fixed records: coordinates, (name, age) pairs, dict keys.", example: `point = (3, 5)\nx, y = point\nprint(x, y)`, mistake: `point[0] = 10  # TypeError: tuples are immutable` },
{ cat: "Data Structures", topic: "Dictionaries", syntax: `student = {"name": "Ana"}\nstudent["age"] = 21`, explanation: "Key → value pairs. Fast lookup by key; keys are usually strings or numbers.", when: "Records, lookups, counters, JSON data.", example: `student = {"name": "Ana"}\nprint(student["name"], student.get("age", 0))`, mistake: `student["grade"]  # KeyError when missing; use .get("grade", default)` },
{ cat: "Data Structures", topic: "Dictionary methods", syntax: `keys(), values(), items(), get(), pop()`, explanation: "items() loops pairs, get() reads safely, pop removes a key.", when: "Iterating or reading dictionaries without crashes.", example: `d = {"a": 1, "b": 2}\nfor k, v in d.items():\n    print(k, v)`, mistake: `Looping with for k in d: then d[k] is fine, but mixing .items() and indexing confuses beginners.` },
{ cat: "Data Structures", topic: "Sets", syntax: `unique = {1, 2, 3}\nunique.add(4)`, explanation: "Unordered collection with no duplicates. Fast membership tests and math like union and intersection.", when: "Removing duplicates, checking membership, comparing groups.", example: `nums = [1, 2, 2, 3]\nprint(set(nums), 2 in set(nums))`, mistake: `items[0]  # sets have no order, so no indexing` },
{ cat: "Data Structures", topic: "Nested data", syntax: `students = [{"name": "Ana"}, ...]\nstudents[0]["name"]`, explanation: "Lists of dicts (or dicts of lists) model real records. Chain the lookups.", when: "Databases, APIs, JSON, game entities.", example: `students = [{"name": "Ana", "grade": 90}]\nprint(students[0]["name"])`, mistake: `students["name"]  # the outer container is a list: index it first` },
{ cat: "Data Structures", topic: "List comprehension", syntax: `[x * 2 for x in items if x > 0]`, explanation: "Builds a list in one line: expression, loop, optional filter.", when: "Transforming or filtering lists concisely.", example: `squares = [x * x for x in range(5)]\nprint(squares)`, mistake: `[x * 2 for x in items]:  # no colon in a comprehension` },
{ cat: "Data Structures", topic: "Sorting", syntax: `sorted(items)\nsorted(items, reverse=True)\nitems.sort()`, explanation: "sorted() returns a new list; .sort() orders in place. Both take key= for custom rules.", when: "Ranking, ordering records, top-N lists.", example: `nums = [3, 1, 2]\nprint(sorted(nums), nums)`, mistake: `nums = nums.sort()  # assigns None; .sort() changes nums itself` },
{ cat: "Functions", topic: "def", syntax: `def name():\n    # body`, explanation: "Defines a reusable function. The body runs only when called.", when: "Grouping repeated logic, organizing programs.", example: `def welcome():\n    print("Hi!")\nwelcome()`, mistake: `def name()\n    # missing colon` },
{ cat: "Functions", topic: "Parameters", syntax: `def greet(name, greeting="Hi"):\n    ...`, explanation: "Inputs a function accepts. Defaults make parameters optional.", when: "Making functions flexible and reusable.", example: `def greet(name, greeting="Hi"):\n    print(f"{greeting}, {name}")\ngreet("Ana")\ngreet("Bo", "Yo")`, mistake: `Calling greet() when name has no default → TypeError.` },
{ cat: "Functions", topic: "return", syntax: `def add(a, b):\n    return a + b`, explanation: "Sends a value back to the caller and exits the function. No return → returns None.", when: "When the result is needed elsewhere: print, assign, chain.", example: `def add(a, b):\n    return a + b\nresult = add(2, 3)\nprint(result)`, mistake: `Using print() inside and expecting a value outside — print returns None.` },
{ cat: "Functions", topic: "lambda", syntax: `double = lambda x: x * 2`, explanation: "A one-expression anonymous function. Often passed into map, filter or sorted(key=...).", when: "Tiny throwaway functions.", example: `add = lambda a, b: a + b\nprint(add(2, 3))`, mistake: `lambda x: return x * 2  # no return keyword in lambdas` },
{ cat: "Functions", topic: "map & filter", syntax: `list(map(fn, items))\nlist(filter(fn, items))`, explanation: "map applies fn to every item, filter keeps items where fn is True. Both return iterators — wrap in list().", when: "Data transformation pipelines.", example: `nums = [1, 2, 3]\nprint(list(map(lambda x: x * 10, nums)))\nprint(list(filter(lambda x: x > 1, nums)))`, mistake: `map(...) without list() around it prints a map object, not values.` },
{ cat: "Functions", topic: "Scope", syntax: `def f():\n    local = 1\n    return local`, explanation: "Names created inside a function are local and vanish after it returns. Globals are readable but need `global` to reassign.", when: "Avoiding name collisions; understanding NameError.", example: `def f():\n    x = 5\n    return x\nprint(f())`, mistake: `Using a local variable outside the function → NameError; return it instead.` },
{ cat: "Functions", topic: "*args & **kwargs", syntax: `def f(*args, **kwargs):\n    ...`, explanation: "Collects extra positional arguments into a tuple and keyword arguments into a dict.", when: "Flexible APIs, wrappers, decorators.", example: `def total(*nums):\n    return sum(nums)\nprint(total(1, 2, 3))`, mistake: `Mixing up *args (positional) with **kwargs (named).` },
{ cat: "Advanced", topic: "Classes", syntax: `class Dog:\n    def __init__(self, name):\n        self.name = name`, explanation: "A blueprint for objects. __init__ builds each object; self is the object itself.", when: "Modelling things with state and behaviour: users, accounts, pets.", example: `class Dog:\n    def __init__(self, name):\n        self.name = name\n    def speak(self):\n        return f"{self.name}: woof"\nprint(Dog("Rex").speak())`, mistake: `def __init__(name):  # forgot self` },
{ cat: "Advanced", topic: "Inheritance", syntax: `class Dog(Animal):\n    ...`, explanation: "A child class inherits the parent's methods and can override them. super() calls the parent.", when: "Shared behaviour with specializations.", example: `class Animal:\n    def speak(self): return "..."\nclass Dog(Animal):\n    def speak(self): return "Woof!"\nprint(Dog().speak())`, mistake: `class Dog(Animal)  # missing colon after the parent` },
{ cat: "Advanced", topic: "try / except", syntax: `try:\n    risky()\nexcept ValueError:\n    handle()`, explanation: "Catches errors so the program can recover instead of crashing. You can catch several types and use finally.", when: "User input, file/network operations, divisions.", example: `try:\n    print(int("abc"))\nexcept ValueError:\n    print("Not a number")`, mistake: `Bare except: hides every bug — catch the specific error.` },
{ cat: "Advanced", topic: "raise", syntax: `raise ValueError("bad input")`, explanation: "Throws an error on purpose, with a message. Useful for invalid data.", when: "Validating inputs in functions and classes.", example: `def set_age(age):\n    if age < 0:\n        raise ValueError("Age cannot be negative")\n    return age\nprint(set_age(5))`, mistake: `raise "string"  # must raise an exception type` },
{ cat: "Advanced", topic: "Modules & import", syntax: `import math\nfrom math import sqrt`, explanation: "Imports code from the standard library or your own files. Access with module.name, or import names directly.", when: "math, random, json, datetime — anything beyond the basics.", example: `import math\nprint(math.sqrt(16))`, mistake: `import math\nprint(sqrt(16))  # NameError: it is math.sqrt` },
{ cat: "Advanced", topic: "File handling", syntax: `with open("data.txt", "w") as f:\n    f.write("hello")`, explanation: "Opens files for read (r) or write (w). The with block closes the file automatically.", when: "Saving and loading data on a real computer (sandbox is virtual — use JSON strings here).", example: `# with open("notes.txt") as f:\n#     text = f.read()\nprint("File pattern: open, read/write, close")`, mistake: `open("x.txt") without closing it — always use with.` },
{ cat: "Advanced", topic: "JSON", syntax: `import json\ntext = json.dumps(obj)\nobj = json.loads(text)`, explanation: "dumps converts Python objects to a JSON string; loads converts back. The standard format for saved data and APIs.", when: "Saving settings, sending data to web services.", example: `import json\ntext = json.dumps({"a": 1})\nprint(text, json.loads(text)["a"])`, mistake: `json.loads(file)  # loads takes a string; load() takes a file object` },
{ cat: "Advanced", topic: "Decorators", syntax: `@decorator\ndef func(): ...`, explanation: "A function that wraps another to add behaviour (logging, timing, checks) without editing it.", when: "Cross-cutting features applied to many functions.", example: `def announce(fn):\n    def wrapper():\n        print("start")\n        fn()\n    return wrapper\n@announce\ndef task(): print("work")\ntask()`, mistake: `Forgetting to return wrapper from the decorator.` },
{ cat: "Advanced", topic: "Generators", syntax: `def gen():\n    yield 1\n    yield 2`, explanation: "Produces values one at a time with yield, pausing between each. Memory-friendly for big sequences.", when: "Large or infinite sequences, streaming data.", example: `def gen(n):\n    for i in range(n):\n        yield i * i\nprint(list(gen(3)))`, mistake: `Using return instead of yield — then it is a normal function.` },
{ cat: "Advanced", topic: "Recursion", syntax: `def fact(n):\n    if n <= 1: return 1\n    return n * fact(n - 1)`, explanation: "A function that calls itself on a smaller problem, with a base case that stops it.", when: "Trees, nested structures, divide-and-conquer math.", example: `def fact(n):\n    if n <= 1: return 1\n    return n * fact(n - 1)\nprint(fact(5))`, mistake: `No base case → RecursionError (infinite recursion).` },
{ cat: "Advanced", topic: "String methods", syntax: `s.upper()  s.strip()  s.split()  "-".join(list)`, explanation: "split breaks a string into a list, join glues a list into a string, strip trims spaces, replace swaps text.", when: "Parsing input, cleaning data, formatting output.", example: `text = "  Hello World  "\nprint(text.strip().lower().replace("world", "python"))`, mistake: `s.split("-") when the text has no "-" gives a one-item list.` },
{ cat: "Advanced", topic: "datetime", syntax: `from datetime import datetime\nnow = datetime.now()`, explanation: "Dates and times: now(), formatting with strftime, arithmetic with timedelta.", when: "Logs, timestamps, ages, schedules.", example: `from datetime import datetime\nprint(datetime(2026, 1, 1).strftime("%Y-%m-%d"))`, mistake: `Comparing datetime with a plain string — parse it first.` },
{ cat: "Advanced", topic: "random", syntax: `import random\nrandom.randint(1, 6)`, explanation: "Random numbers and choices: randint (inclusive range), choice (pick one), shuffle (reorder).", when: "Games, simulations, sampling, test data.", example: `import random\nrandom.seed(1)\nprint(random.randint(1, 6), random.choice(["a", "b"]))`, mistake: `random.randint(1, 6) includes both ends — not like range().` },
{ cat: "Advanced", topic: "enumerate & zip", syntax: `for i, item in enumerate(items): ...\nfor a, b in zip(list1, list2): ...`, explanation: "enumerate pairs each item with its index; zip walks two lists in parallel.", when: "Numbered output, pairing related sequences.", example: `names = ["a", "b"]\nfor i, name in enumerate(names, start=1):\n    print(i, name)`, mistake: `zip stops at the shorter list — extra items are dropped.` },
{ cat: "Advanced", topic: "Truthiness", syntax: `if items:   # non-empty?\nif name:    # non-empty string?`, explanation: "Empty things (0, \"\", [], {}, None) are False; everything else is True.", when: "Writing clean checks without == comparisons.", example: `items = []\nprint(bool(items), bool([0]), bool("0"))`, mistake: `if len(items) > 0: works, but if items: is the Pythonic way.` },
/*APPEND*/
]
